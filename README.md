# pure
What's inside:

Purple Reign.html + tweaks-panel.jsx — open directly in a browser, no build step

vendor/ — React, ReactDOM, Babel (all pre-downloaded, no CDN needed)

fonts/ — DM Sans + JetBrains Mono woff2 files (self-hosted ready)

pure-vite/ — full production Vite project for GitLab Pages

HANDOFF.md — step-by-step guide for Claude Code to wire up your API

API_CONTRACT.md — exact JSON shapes for every endpoint

ASSETS.md — fallback instructions for fonts/libs if private repos don't have them

Tell Claude Code to start with HANDOFF.md — it walks through the data mapping, auth setup, CORS config, and GitLab Pages deployment in order.
